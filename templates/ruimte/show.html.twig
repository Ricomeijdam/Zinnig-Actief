{% extends 'base.html.twig' %}

{% block title %}Ruimte{% endblock %}

{% block body %}
    <h1>Ruimte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ruimte.id }}</td>
            </tr>
            <tr>
                <th>Locatie</th>
                <td>{{ ruimte.locatie }}</td>
            </tr>
            <tr>
                <th>Plekken</th>
                <td>{{ ruimte.plekken }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ruimte_index') }}">back to list</a>

    <a href="{{ path('ruimte_edit', {'id': ruimte.id}) }}">edit</a>

    {{ include('ruimte/_delete_form.html.twig') }}
{% endblock %}
